import styled from 'styled-components';

const Wrapper = styled.div`
  margin: 10px 0;
  font-family: Lato;
  text-align: center;
  overflow-x: auto;
  overflow-y: hidden;
`;

export default Wrapper;
