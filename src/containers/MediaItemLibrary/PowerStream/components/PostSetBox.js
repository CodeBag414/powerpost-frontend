import React from 'react';
import PropTypes from 'prop-types';
import styled from 'styled-components';

import PostSetList from './PostSetList';

const Wrapper = styled.div`
  position: absolute;
  top: 60px;
  bottom: 0;
  left: 0;
  right: 0;
`;

const PostSetBox = ({  }) => (
  <Wrapper>
    
  </Wrapper>
);

PostSetBox.propTypes = {
  
};

export default PostSetBox;
